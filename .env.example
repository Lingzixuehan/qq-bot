# NoneBot2 配置
HOST=127.0.0.1
PORT=8080
LOG_LEVEL=INFO
SUPERUSERS=["123456789"]  # 超级管理员QQ号列表

# OneBot 配置
ONEBOT_ACCESS_TOKEN=""  # 如果NapCat设置了token，填写在这里

# 命令前缀（只允许 / 开头的命令，避免误触发）
COMMAND_START=["/"]  # 命令前缀，必须使用 /help 格式
COMMAND_SEP=[" "]  # 命令分隔符
